# ── Core web framework ────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0
starlette>=0.40.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20.0
asyncpg>=0.30.0
# asyncpg is only needed for PostgreSQL (Supabase).
# SQLite works without it. Install when using Postgres:
#   pip install "asyncpg>=0.30.0"

# ── Pydantic (Python 3.14 compatible) ────────────────────────────────────────
pydantic>=2.11.0
pydantic-settings>=2.5.0

# ── AI / HTTP ─────────────────────────────────────────────────────────────────
openai>=1.35.0
httpx>=0.27.0
Pillow>=10.4.0

# ── Payments ──────────────────────────────────────────────────────────────────
stripe>=9.12.0

# ── Database migrations ───────────────────────────────────────────────────────
alembic>=1.13.0

# ── Auth ──────────────────────────────────────────────────────────────────────
bcrypt>=4.1.3
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0
python-multipart>=0.0.9

# ── Redis / caching ───────────────────────────────────────────────────────────
redis>=5.0.0
prometheus-fastapi-instrumentator>=7.0.0

# ── Email ─────────────────────────────────────────────────────────────────────
aiosmtplib>=3.0.0

# ── Background tasks (worker process only) ───────────────────────────────────
celery>=5.4.0

# ── Browser agent (optional) ─────────────────────────────────────────────────
# After install, run: .venv\Scripts\playwright install chromium
playwright>=1.44.0
playwright-stealth>=1.0.6

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
